# FOR WINDOWS ONLY
version: '3.11'

services:

    django:
        container_name: django
        build:
            context: ./
        command: python manage.py runserver 0.0.0.0:8000
        volumes: 
         - ./helloWorld/testBranch:/usr/src/app/
        ports:
         - 8001:8000
        environment:
         - DEBUG=1
         - SECRET_KEY=sigmasigmasigma
         - ALLOWED_HOSTS=localhost, 127.0.0.1
    